# 開發／測試依賴。
# 完整既有 suite：python -m unittest discover -s tests
# 完整 pytest suite（含 packaging／bundle／startup 與 GUI pilot）：
#   $env:QT_QPA_PLATFORM = 'offscreen'
#   python -m pytest tests -q
# 其中 test_pytest_qt_runtime.py 與 test_reward_gui_pilot.py 是兩支 offscreen GUI pilot，
# 但不是完整 pytest suite 的全部。
# 版本為 2026-07-22 已驗證組合。完整測試環境必須先安裝本檔依賴；
# 缺 pytest 等套件時，test module 可能在 discovery 的 import 階段直接失敗。

# Runtime／完整既有 suite
PySide6==6.11.1
matplotlib==3.11.1
pypdf==6.10.0
reportlab==4.4.9

# 完整 pytest suite
pytest==9.1.1
pytest-qt==4.5.0
