# 開發／測試依賴。
# 完整既有 suite：python -m unittest discover -s tests
# pytest-qt pilot（offscreen）：
#   $env:QT_QPA_PLATFORM = 'offscreen'
#   python -m pytest -q tests/test_pytest_qt_runtime.py tests/test_reward_gui_pilot.py
# 版本為 2026-07-22 pilot 已驗證組合；缺 pytest 時 pytest 專用測試會被 unittest 自動跳過。

# Runtime／完整既有 suite
PySide6==6.11.1
matplotlib==3.11.1
pypdf==6.10.0
reportlab==4.4.9

# pytest pilot
pytest==9.1.1
pytest-qt==4.5.0
