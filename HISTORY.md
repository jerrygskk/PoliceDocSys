# 版本記錄

> 版本號單一來源 `lib/version.py` 的 `__version__`。**進版用 `python tools/bump_version.py <版號>`**（版號自帶不自動進位；同時改 `version.py` 與產 `version_info.txt`）。本表與 git tag（`v{__version__}`）手動對齊。⚠️ 勿手改 `version.py`，否則 `version_info.txt` 不同步。
>
> 本表只留「改了什麼、為什麼」的骨架，細節查對應 tag 與 `git log`。

| 版本 | 摘要 |
|------|------|
| v1.2.10 | **日期欄防呆：擋掉誤改途徑並在送出前確認，無 schema 變動**。起因是現場事故：某日刑案陳報 12 筆只印得出 4 筆，8 筆的 `report_date` 年份被誤寫成隔年，而簽收表只撈「陳報日期＝所選日期」。送文者模式下日期欄連續登錄共用，錯一次就一路錯到底。①所有 `QDateEdit` 停用滾輪與方向鍵並強制 `NoButtons`，改期路徑只剩打數字與月曆挑；點擊改到年份的根因是 `calendarPopup=True` 與 spin 箭頭座標對不起來（PITFALLS QTW-13／QTW-14）。②送出前確認 `ui_utils/date_guard.py`，只提示不擋，同頁同欄同日期本次執行只問一次，接在六個送出點。③離線測試會被這個 modal 卡住，根 `conftest.py` 統一自動確認（PITFALLS TST-4）。pytest 1026＋49＋9。<br>**⟪同版號重新發布⟫**④「自助取號模式」更名「發文結算模式」，程式識別字與 `report_mode_*` key 刻意不改名。⑤陳報頁補上模式提示條（放 `Layout3.ui` row0 col7／col8，避開 LAY-2b）。⑥日期防呆的「本次已確認」改為各頁分開記（`scope`）。⑦陳報預覽日期改 `MM/DD`。<br>**v1.2.10-v2**：敘獎登錄的修改視窗補上唯讀的發文日期／發文人員兩列，與罰單一致；儲存路徑未動。 |
| v1.2.9-v6 | **同版號重新發布：自動化驗收擴充，無功能／schema／UI 變動**。①新增六支 GUI pilot（連同敘獎共八支），走完整條使用者流程：登出即關窗、歸檔正名、發文結算到結算發文、跨年度重置、備份還原、回收筒還原、設定面板接線。②釘住兩條不變式——未發文哨兵各流程不一致（陳報 NULL、敘獎與罰單空字串），故 pilot 一律用真實分頁送出不以 SQL 塞資料；跨年度重置的稽核必須寫在備份之前。③設定面板改以消費端讀取函式斷言，而非直接查 `App_Settings`。④每支交付前都以「故意破壞、確認會紅」反證。⑤PITFALLS 新增 TST-6。pytest 995＋49＋9。 |
| v1.2.9-v5 | **同版號重新發布：登出行為改為「降權即關窗」，無 schema 變動**。登出／閒置登出／改密碼後自動登出時直接關閉所有開啟中的對話框（Qt modal 有自己的事件迴圈，原本回到 handler 仍會用舊權限往下做）；未完成輸入一律丟棄。只在真的失去分頁時關窗，升權不關；關窗後當前頁仍可見就留在原頁，否則導向登入並記住原頁。⚠️ 原生檔案對話框關不掉，那幾條流程仍倚賴「執行時權限複核」。 |
| v1.2.9-v4 | **同版號重新發布：測試與驗收機制強化，無功能變動**。①shell 測試層改行程隔離（`ISOLATED_MODULES` 內每支測試各派一個子行程），累積量由 28 降為 1；⚠️ 是隔離不是修復，長壽掛載未拆，判讀規則改為 native crash 一次即視為回歸。②新增隔離清單釘住測試與 AST 掃描防漏列。③`print_baseline.py --check` 改環境先行，環境漂移停在算圖之前，結束碼分家（1 影像差異／2 基準缺失／3 環境漂移）。 |
| v1.2.9-v3 | **同版號重新發布：PACKED.zip 改四檔＋新增網路分享捷徑工具**。①zip 內兩支 exe 改固定英文名不帶版號（避開 SMB 中文檔名問題），與空白 `dbfile.db`、BAT 四檔同層。②新增 `packaging/create_shared_shortcuts.bat`，自動解析 UNC 建立兩支中文捷徑，支援四種擺法；以 cmd 呼叫 PowerShell 讀自己執行，不產生 `.ps1` 故不受 ExecutionPolicy 限制，編碼與換行由 `.gitattributes` 釘住。③瀏覽頁無 PDF 圖示的主旨改存完整文字（原本先截短，tooltip 與複製都被截）。 |
| v1.2.9-v2 | **同版號重新發布：權限最後防線與驗證機制收斂**。①跨年度重置與歸檔兩入口在 modal 返回後再確認一次身分，歸檔另補 DB 失敗時的檔名還原。②排序儲存補 `_refEditable()` 並回傳成功／失敗，降權即放棄未存排序。③罰單改五欄原值 CAS 並強制傳開窗快照，衝突與已刪除分流。④pytest 正式 gate 改五層 marker 兩段跑，文件契約獨立成 `test_release_documentation_contract.py`。⑤列印基準改用全虛構資料建暫存 DB，matplotlib 固定 3.11.1。⑥PII gate 改掃工作樹／index／HEAD。 |
| v1.2.9 | **簽收單列印引擎改用 Qt 原生繪圖＋罰單簽收表版面對齊**。①三個出口全面改走 `lib/print_canvas.py` 的 `QtCanvas`，產品路徑不再有 matplotlib；版面刻意零變動，以三層驗收網把關。⚠️ 期間踩到兩個 op log 抓不到的雷：`msjh.ttc` 與 `msjhbd.ttc` family 名相同導致靠名字切粗體靜默全滅（須明確 `setBold()`）、垂直置中須沿用 `"lp"` 參考字串規則。②完整版 exe 55.5→30.1 MB。③罰單簽收表粗外框改從標題帶起算，上邊界與其他四張齊平。④結算清單敘獎「承辦人」欄改取受獎人首位。⑤立新規則：任何視窗初始焦點不得停在日期欄位。 |
| v1.2.8-v2 | **同版號重新發布：打包瘦身與開機加速，無功能變動**。①排除 PDF／網路整串與備用平台外掛、冷門影像格式（判定依據是解析 PE import table 反查誰在連結，不是看原始碼有沒有 import）。②`drop_openssl=True` 獨立版限定砍 OpenSSL。③移除 `LoadWorker` 每步 `sleep(0.05)` 的除錯延遲（開機平白多 0.3～0.45 秒）。④獨立版橫幅重壓。體積：完整版 60.1→55.5 MB、獨立版 34.3→26.6 MB。 |
| v1.2.8 | **敘獎發文頁併回結算發文＋陳報預覽版面修正**。①敘獎發文整頁移除，發文回歸列印頁「結算發文」；大 Tab 由 11 減為 10，`Layout10.ui` 留空號。②敘獎重新掛回陳報模式；⚠️ reward 不吃舊全域 key 的回退，避免舊庫殘值誤啟。③唯讀鎖由七把減為六把。④候選人員名條改照 `sort_order`，取消依使用次數重排。⑤陳報兩張預覽表改固定欄寬、伸縮欄改為「陳報主旨」、`previewLayout` 分配 3:2。⑥修掉三個版面雷：模式切換右半位移、resize 不重算欄寬、開啟即冒水平捲軸。 |
| v1.2.7 | **依角色隱藏主功能 TAB**。可見矩陣為純邏輯 `visibleTabKeys()`，只能縮小目前 Profile 的可見集合，未知角色退回最小範圍；執行期只用 `setTabVisible()`，固定 index 映射不變（不得 `removeTab()`）。主選單維持全部入口，選到看不到的功能改導向設定頁登入並記住目標，登入成功且可見才前往。 |
| v1.2.6-v2 | **同版號重新發布**。①結算發文日期改可選（預設今天、允許回填），簽收表依實際結算日產生。②罰單簽收歸屬一律依 `register_date`，修掉切模式後結算會從所有簽收表消失的漏單。③全域 `QDateEdit` 滾輪 guard。④敘獎編輯與批次發文加原值比對，防多機無聲覆蓋。⑤打包驗證全面 fail-closed，PE import 名稱補 `.drv` 等副檔名（`WINSPOOL.DRV` 曾使完整版整支誤報）；發布 gate 移到 push／tag 之前。 |
| v1.2.6 | **獨立版正式命名與雙程式發布＋UI 文字全面校訂**。①兩支程式定名，名稱單一來源為 `lib/app_profile.py`（主選單標題原沿用 `.ui` 寫死的完整版名稱，獨立版顯示錯名）。②罰單「發文者」一律改稱「發文人員」。③修正可打字下拉的 focus-in 延後清空會吃掉當下選取。④罰單稽核政策改為只記刪除。⑤HELP 修正獨立版按 ? 開到別頁（改依分頁 key 換算頁碼）。⑥發布改為 5 個 asset。 |
| v1.2.5 | **罰單登錄（第 5 張公文表）＋敘獎唯讀鎖破口修補**。新增 `Document_Ticket` 與 `Document_Ticket_Full`，CRUD 寫入收斂於 `lib/ticket_utils.py`；罰單接入結算、列印、瀏覽、跨年度重置與備份統計五條共用管線，**刻意不進資源回收筒**。敘獎登錄／發文改為兩個獨立唯讀鎖，瀏覽頁敘獎編輯收斂為僅 admin。預覽表伸縮欄改由最右空標題欄吸收（PITFALLS LAY-7）。 |
| v1.2.4 | **測試基線盤點＋公開契約強化＋pytest-qt 敘獎 pilot**。建立逐檔基線與 DB 契約矩陣，缺產物情境改為可辨識 skip；既有高耦合測試改以公開行為驗證；新增 offscreen runtime smoke 與單一敘獎 GUI 生命週期 pilot（受控破壞能在 DB mutation 前紅燈，未新增 production seam）；新增並鎖定 `requirements-dev.txt`。其餘 GUI 流程擴充須另立核准計畫。 |
| v1.2.3 | **敘獎三態語意集中＋發文預查失敗改為停止**。①預查 SQL 例外原本被吞掉後照樣彈確認視窗，改為 `reportError` 後 `return`，避免少了「將覆蓋原發文日期」提示而誤覆蓋。②三態條件集中於 `lib/db_utils.py`（`REWARD_ACTIVE_SQL` 等＋`rewardState()`），各處魔法條件逐一替換。③瀏覽頁「未發文」橘字改由 `TABLE_META` 的 `pending_date_col` 宣告，移除散落的類型判斷。 |
| v1.2.2 | **結算併發防護＋敘獎彈窗選人快照修正**。①結算 UPDATE 加「仍未發文」條件（敘獎用 `= ''`，**不可用 `IS NULL`**——NULL 是軟刪除哨兵），他機已處理的列 rowcount=0 自然跳過，實結數少於勾選數時提示、不 rollback。②敘獎人員欄快照改在 `showPopup` 瞬間抓取，修 completer 與下拉混用會洗掉已選人名（PITFALLS QTW-11）。③三彈窗日期驗證收斂 `_resolveReportDate`。 |
| v1.2.1 | **敘獎接入自助取號＋發文人員欄、結算彈窗重構**。①敘獎未發文哨兵用 `''`（**不可用 NULL**，那是軟刪除哨兵）。②`Document_Reward` 加 `sender_id`，瀏覽頁補發文人員欄、排序改升冪。③結算彈窗重構為單一表格＋`SETTLE_META` registry（新型態接結算＝加一筆 meta）。④三編輯彈窗發文日期改 `NullableDateEdit`，空白＝未發文哨兵。⑤穩定性三項：`last_modified` 索引、每週深度完整性檢查、瀏覽頁列同步測試。已知迴避：深色模式 tooltip 黑底無解（QSS-7），新功能不依賴 tooltip。 |
| v1.2.0 | **全新 repo 起點（`PoliceDocSys`）**：內容延續 v1.1.12、功能無變動；為清除舊 git 歷史中的真實人名，改以單一初始 commit 建立全新公開 repo（舊 `project_police` 轉私有）。版號自 1.2.0 起、第三碼歸零。以下 v1.1.x 及更早為文件保留的歷史記錄，該版 git 歷史已不在本 repo。 |
| v1.1.12 | **敘獎登錄（Tab3）＋罰單登錄佔位頁（Tab4）**：全 Tab 重編號 0–9。新增 `Document_Reward` 表與登錄頁（三身分皆可操作、無角色 gate），瀏覽頁 reward 子頁、簽收單列印敘獎 section＋`print_title_reward`，軟刪除／回收筒／備份統計／跨年度重置皆已接入。主視窗移除 `maximumSize` 鎖定改為可放大。code review 修正：修改彈窗改繼承 `_BaseEditDialog`、被他機刪除時彈白話提示、UPDATE 影響 0 列不再假成功。 |
| v1.1.11 | **發文頁「已輸入未發文」提醒條**（`_pending` set 追蹤掃入未發文的 doc_id，淺琥珀提醒條即時消長）。**已發文即時鎖修改**（權限漏洞）：發文後重算編號欄可點狀態，`_onEditRow` 另以 DB `dispatch_date` 為準硬 gate。GFS 加每月層（`MONTHLY_KEEP=12`）。`getConn` 加 `busy_timeout=3000`（多機 SMB 鎖碰撞自動重試，不開 WAL）。結算修正：隱藏但仍勾選者照結（原靜默漏結）；結算不寫稽核。 |
| v1.1.10 | **案類別名（俗稱搜尋）**：`Ref_CaseTypes` 加 `alias` 欄，三個案類下拉輸入俗稱即出現「俗稱 → 正式案類」候選，選取回填正式案類；歸檔比對亦納入計分。可打字下拉提示字改 `attachComboHint`（修「打字黏提示字」）。案類改名不寫操作紀錄（`audit_name_change` 旗標；新增與停用照記）。陳報頁輸入欄加寬 25%、送出鈕移至子頁籤同列右端。 |
| v1.1.9 | **自助取號模式（陳報模式二選一）**：新增 `InputModePanel`（存 `report_input_mode`，即時生效、Reset 不清）。自助模式下承辦人僅取文號，`report_date`／`sender_id` 存 NULL＝未發文；送文者於列印頁按「結算發文」（`SettleDialog`）批次補值並寫稽核，結算成功後自動產生簽收表。未發文不進歸檔；瀏覽頁陳報日期 NULL 顯示橘字「未發文」。 |
| v1.1.8 | **案類互轉**：瀏覽頁修改視窗新增「⇄ 轉換類別」鈕（僅管理身分），補填目的案類必填欄位、列出將被捨棄的來源獨有欄位，確認後原編號作廢並產生新編號，電子檔隨同搬移改名；轉換不可逆、無二次確認框。核心邏輯 `lib/doc_convert.py`（純邏輯＋單元測試）。轉換後瀏覽頁改差異更新取代整表重載。 |
| v1.1.7 | **資料安全強化**。①異地備份：`run_auto_backup(extra_dirs=)` 對第二位置同步跑 GFS，各處獨立 try；設定存 `backup_second_dir`。②啟動 `quick_check`＋開機救援對話框：損毀時跳過當日備份（保留既有好備份）並進 `runStartupRescue`，自動找最新可用備份、以**備份檔**內的 hash 驗管理者密碼。③備份還原子頁（僅 admin）整合三來源清單＋驗完整性＋還原流程。④閒置強制關閉前 90 秒亮紅色倒數橫幅，總時限不變。⑤LOADING 移除置頂旗標（錯誤視窗不再被壓在後面）。 |
| v1.1.6 | **跨年度後唯讀模式**：四個獨立開關停用一般使用者對四種輸入流程的操作，硬 gate 在送出進入點、UI 反灰＋紅色橫幅為輔助。**系統設定子頁**：QScrollArea 直排四個嵌入面板取代原本兩顆 nav 鈕＋兩個 Dialog，各面板獨立「儲存」（未變動反灰、改值即亮）。閒置逾時改為可設定。變更密碼防兩組相同（兩組同值時 `archive` 永遠登不進）。重構：唯讀相關收斂為 `InputLockMixin`、四面板抽共用基底 `_SettingsPanel`。 |
| v1.1.5 | **多機共用穩定性**：閒置強制關閉由 20 分改 **14 分半**——DB 與鎖檔在 SMB 網路碟，須搶在 Windows 15 分鐘鎖螢幕之前關閉並清鎖檔，否則背景續更新心跳會卡住別台電腦登入。**權限修正**：歸檔管理可經序號欄雙擊行內編輯改排序再存回＝權限繞過，補 `_refEditable()` guard。瀏覽表格改 `NoSelection`＋`NoFocus`。重構：六個參照項 add/edit 收斂為 `_addRef`／`_editRef`，回收筒抽成 `TrashPanel`。 |
| v1.1.4 | **安全性**：登入密碼比對改 `secrets.compare_digest`；歸檔 `os.rename` 前以 `os.path.commonpath` 二次驗證落點。變更密碼後自動登出。**可空白日期框治本**：查獲日期等欄位由 QDateEdit＋哨兵改為 `NullableDateEdit(QLineEdit)`。重構：錯誤彈窗統一走 `reportError`（原本把 SQLite 英文原文丟給使用者），獨立稽核事件抽 `writeAuditSafe`。 |
| v1.1.3 | **內部重構為主，無使用者新功能**。schema／種子改為程式碼唯一來源：DDL 收進 `lib/db_schema.py`、種子收進 `lib/db_seed.py`，新增 `tools/gen_shell_db.py` 產乾淨空殼，取代發版「從 git HEAD 取二進位」；單元測試改用同一份 schema，消除走鐘隱患。`dbfile.db` 改為不入庫，並以 `git filter-repo` 抹除歷史中含資料的舊檔。 |
| v1.1.2 | **簽收表標題可自訂**（存 `App_Settings` 四 key、未設定走 `○○` 預設＋列印頁紅字提醒、Reset 不清）。簽收表欄內換行改用真實字型度量（修欄寬還夠卻提早折行）。權限修正：收文／陳報頁刪除誤擋成僅 admin，改回一般使用者可刪。四處刪除流程收斂為 `softDeleteDoc`。簽收表 PDF 改前景產生＋「產生中」popup（消除偶發崩潰）。閒置自動關閉改 `os._exit` 硬關（穿透 modal）。通用 UI 自 `db_utils` 搬至 `ui_utils/ui_common.py`。 |
| v1.1.1 | **誤刪還原（資源回收筒）**：刪除前把整列快照存入 `Trash_Documents`，設定頁新增子頁（僅 admin）可單選還原並保留刪除當下歸檔狀態；Reset 一併清空。**啟動冪等建表 `ensureSchema`**：附加式結構改於啟動時自動套用，新增資料表不再需要發 fix 工具。主選單打包版偶被壓住，改顯示後強制拉到最前。 |
| v1.1.0 | **稽核大版本（含現場升級工具）**。三角色權限＋操作稽核（`Audit_Log`＋操作紀錄 Tab，僅 admin）。效能：瀏覽頁 cellWidget 改純 item＋啟動預載建表。安全性：錯誤訊息白話化、閒置自動登出與自動關閉、APP 層軟性互斥（`dbfile.lock` 勸導）。平時自動備份 GFS 輪替。⚠️ 升級舊庫前置：先 `fix_cat_status` 再 `fix_audit_setup`。 |
| v1.0.9 | **發文分類／案件狀態正規化**：顯示名去字母前綴縮為兩字，View 撈出即顯示，移除程式端 `_STATUS_MAP`／`_CAT_MAP`；現行犯判斷改以 `case_status` ID（`CS01`）比對。HELP 說明內按鈕改用預烤圓角 SVG。 |
| v1.0.8 | **程式內 HELP**：每個大 Tab 右上角新增說明鈕開該頁彈窗（七頁），內容單一來源 `ui_utils/help_content.py`、彈窗 `ui_utils/help_dialog.py`。 |
| v1.0.7 | **歸檔頁**：電子檔歸檔成功時連帶標記紙本已歸。**簽收單列印**：開預覽前預設彩色＋長邊雙面。 |
| v1.0.6 | **歸檔頁**：修正承辦人解析會把案由詞與括號內報案人誤判成承辦人。承辦人界定改為「從檔名尾端往前、能對到 DB 人名字典才收，對不到即停」；解析邏輯抽進 `lib/archive_text.py` 並補單元測試。 |
| v1.0.0–1.0.5 | 早期版本：正式版（瀏覽頁 PDF 圖示鈕、歸檔資料夾 UNC）→ 別名初版 → 拖拉排序、歸檔未設定三層警示、瀏覽搜尋改全量載入＋`setRowHidden` → 陳報頁刑案／一般合併單一版面 → 重載鈕、「更新中」提示、檔名過濾、精簡／完整切換鈕；標題列與 exe 顯示版本號。詳見 git tag。 |
